apiVersion: v2
name: api-gateway
description: Helm chart for API Gateway with Istio integration
type: application
version: 1.0.0
appVersion: "1.0.0"

keywords:
  - api
  - gateway
  - istio
  - service-mesh

home: https://example.com
sources:
  - https://github.com/example/api-gateway

maintainers:
  - name: Platform Team
    email: platform@example.com

dependencies:
  - name: istio-base
    version: "1.19.0"
    repository: "https://istio-release.storage.googleapis.com/charts"
  - name: istiod
    version: "1.19.0"
    repository: "https://istio-release.storage.googleapis.com/charts"
