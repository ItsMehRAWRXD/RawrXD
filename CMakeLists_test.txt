cmake_minimum_required(VERSION 3.20)
project(PEWriterTest VERSION 1.0)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Add the PE writer test executable
add_executable(pe_writer_test pe_writer_test.cpp)

# Include directories
target_include_directories(pe_writer_test PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

# Link any necessary libraries (none for this test)
# target_link_libraries(pe_writer_test ...)

# Set output directory
set_target_properties(pe_writer_test PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin
)