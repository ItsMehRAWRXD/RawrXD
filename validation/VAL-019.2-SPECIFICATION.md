# VAL-019.2: Tensor Access Correctness

**Status:** Specification Complete, Implementation Pending  
**Date:** 2026-07-17  
**Depends on:** VAL-019.1 ✅

---

## Purpose

Prove that RawrXD can correctly access tensor data described by the GGUF artifact.

**Scope:**
- ✅ Tensor offset within file bounds
- ✅ Tensor byte size calculation
- ✅ Raw byte extraction
- ✅ Tensor checksum
- ✅ Repeat extraction determinism

**Not in Scope:**
- ❌ Decompression
- ❌ Dequantization
- ❌ Memory mapping
- ❌ Tensor execution

---

## Validation Gates

### T1: Tensor Offset Validation

**Check:** Tensor offset is within file bounds

**Procedure:**
```
for each tensor:
    assert(tensor.offset + tensor.size <= file_size)
```

**Pass Criteria:**
- All 197 tensor offsets valid
- No out-of-bounds access possible

**Evidence:**
```json
{
  "gate": "T1",
  "tensors_checked": 197,
  "valid_offsets": 197,
  "invalid_offsets": 0,
  "passed": true
}
```

---

### T2: Tensor Byte Size Calculation

**Check:** Byte size matches type and shape

**Procedure:**
```
for each tensor:
    calculated_size = calculate_size(type, shape)
    assert(calculated_size == tensor.size)
```

**Type Sizes:**
| Type | Bytes per Element | Block Size |
|------|-------------------|--------------|
| F32 | 4 | - |
| F16 | 2 | - |
| Q4_0 | 0.5 | 32 + 2 |
| Q4_1 | 0.5 | 32 + 2 + 2 |
| Q8_0 | 1 | 32 + 4 |
| Q2_K | ~0.25 | Complex |
| Q3_K | ~0.375 | Complex |
| Q4_K | ~0.5 | Complex |
| Q5_K | ~0.625 | Complex |
| Q6_K | ~0.75 | Complex |
| Q8_K | 1 | Complex |

**Pass Criteria:**
- All tensor sizes correctly calculated
- Quantized types use correct block sizes

**Evidence:**
```json
{
  "gate": "T2",
  "tensors_checked": 197,
  "size_mismatches": 0,
  "passed": true
}
```

---

### T3: Raw Byte Extraction

**Check:** Can read raw bytes at reported offset

**Procedure:**
```
for sample_tensors (10 representative):
    seek to tensor.offset
    read tensor.size bytes
    verify bytes_read == tensor.size
```

**Sample Selection:**
1. token_embd.weight (first tensor)
2. output.weight (last tensor)
3. blk.0.attn_q.weight (layer tensor)
4. blk.15.ffn_gate.weight (middle layer)
5. blk.31.attn_output.weight (last layer)
6. A Q4_0 tensor
7. A Q4_K tensor
8. A Q8_0 tensor
9. A F16 tensor
10. A F32 tensor

**Pass Criteria:**
- All 10 sample tensors readable
- Bytes read matches expected size
- No I/O errors

**Evidence:**
```json
{
  "gate": "T3",
  "samples_tested": 10,
  "samples_passed": 10,
  "bytes_read_total": 1234567890,
  "passed": true
}
```

---

### T4: Tensor Checksum

**Check:** Tensor data checksum is stable

**Procedure:**
```
for sample_tensors (10 representative):
    read tensor bytes
    compute SHA256
    store checksum
```

**Pass Criteria:**
- Checksums computed for all samples
- Checksums are non-zero
- Checksums vary (different tensors have different data)

**Evidence:**
```json
{
  "gate": "T4",
  "samples_tested": 10,
  "checksums": [
    {
      "tensor": "token_embd.weight",
      "sha256": "a91b...",
      "bytes": 123456789
    },
    {
      "tensor": "blk.0.attn_q.weight",
      "sha256": "b42c...",
      "bytes": 987654321
    }
  ],
  "passed": true
}
```

---

### T5: Extraction Determinism

**Check:** Same tensor produces same checksum across runs

**Procedure:**
```
for sample_tensors (10 representative):
    for run in 1..5:
        read tensor bytes
        compute SHA256
    assert(all 5 checksums identical)
```

**Pass Criteria:**
- All 5 runs produce identical checksums for each tensor
- No variance in byte extraction

**Evidence:**
```json
{
  "gate": "T5",
  "samples_tested": 10,
  "runs_per_sample": 5,
  "deterministic_samples": 10,
  "variance_detected": false,
  "passed": true
}
```

---

## Evidence Package

```
validation/val-019.2-evidence/
├── result.json              # Overall validation result
├── gate_t1_offsets.json     # Offset validation details
├── gate_t2_sizes.json       # Size calculation details
├── gate_t3_extraction.json  # Byte extraction details
├── gate_t4_checksums.json   # Tensor checksums
├── gate_t5_determinism.json # Determinism verification
└── samples/                 # Raw tensor samples (optional)
    ├── token_embd.weight.bin
    ├── blk.0.attn_q.weight.bin
    └── ...
```

---

## Example Evidence

### Complete Result

```json
{
  "validation_id": "VAL-019.2",
  "description": "Tensor Access Correctness",
  "timestamp": "2026-07-17T22:00:00",
  "model": "Phi-3-mini-4k-instruct-q8_0.gguf",
  "simulation": false,
  
  "summary": {
    "gates_total": 5,
    "gates_passed": 5,
    "all_passed": true
  },
  
  "gates": {
    "T1": {
      "name": "Tensor Offset Validation",
      "passed": true,
      "tensors_checked": 197,
      "valid_offsets": 197
    },
    "T2": {
      "name": "Tensor Byte Size Calculation",
      "passed": true,
      "tensors_checked": 197,
      "size_mismatches": 0
    },
    "T3": {
      "name": "Raw Byte Extraction",
      "passed": true,
      "samples_tested": 10,
      "samples_passed": 10
    },
    "T4": {
      "name": "Tensor Checksum",
      "passed": true,
      "samples_tested": 10,
      "checksums_computed": 10
    },
    "T5": {
      "name": "Extraction Determinism",
      "passed": true,
      "samples_tested": 10,
      "deterministic_samples": 10,
      "variance_detected": false
    }
  },
  
  "tensor_samples": [
    {
      "name": "token_embd.weight",
      "type": "Q4_0",
      "shape": [32064, 3072],
      "offset": 12345678,
      "bytes": 123456789,
      "sha256": "a91b..."
    },
    {
      "name": "blk.0.attn_q.weight",
      "type": "Q4_K",
      "shape": [3072, 3072],
      "offset": 987654321,
      "bytes": 9876543,
      "sha256": "b42c..."
    }
  ]
}
```

---

## Implementation Notes

### Standalone Validator

Like VAL-019.1, this should be a standalone executable:

```cpp
// tests/val_019_2_tensor_access.cpp
// No dependencies on:
// - gguf_loader.cpp
// - compression codecs
// - dequantization
// - memory mapping
//
// Only depends on:
// - File I/O
// - SHA-256
// - GGUF format constants
```

### Quantized Type Handling

For T2 (size calculation), handle quantized types correctly:

```cpp
size_t calculate_tensor_size(GGMLType type, const std::vector<uint64_t>& shape) {
    size_t num_elements = 1;
    for(auto dim : shape) num_elements *= dim;
    
    switch(type) {
        case GGMLType::F32: return num_elements * 4;
        case GGMLType::F16: return num_elements * 2;
        case GGMLType::Q4_0: return (num_elements / 32) * (32 + 2);
        case GGMLType::Q4_1: return (num_elements / 32) * (32 + 2 + 2);
        case GGMLType::Q8_0: return (num_elements / 32) * (32 + 4);
        // ... etc
    }
}
```

### Sample Selection

Choose diverse samples:
- Different tensor types (F32, F16, Q4_0, Q4_K, Q8_0)
- Different layers (embedding, attention, FFN, output)
- Different positions (first, middle, last)

---

## Success Criteria

### PASS if:
- ✅ All 5 gates pass
- ✅ 197 tensor offsets validated
- ✅ 197 tensor sizes correctly calculated
- ✅ 10 sample tensors successfully extracted
- ✅ 10 tensor checksums computed
- ✅ All extractions deterministic (5 runs identical)
- ✅ Evidence JSON written
- ✅ `"simulation": false`

### FAIL if:
- ❌ Any gate fails
- ❌ Any tensor offset invalid
- ❌ Any size calculation mismatch
- ❌ Any sample extraction fails
- ❌ Non-determinism detected

---

## Relationship to Other Validations

```
VAL-019.1 (Artifact Understanding)
        |
        v
VAL-019.2 (Tensor Access) ⬜ YOU ARE HERE
        |
        v
VAL-019.3 (Embedding Execution)
        |
        v
VAL-019.4+ (Transformer Primitives)
```

VAL-019.2 bridges artifact understanding (VAL-019.1) with tensor execution (VAL-019.3+).

---

## Next Steps

1. **Implement** `tests/val_019_2_tensor_access.cpp`
2. **Compile** standalone validator
3. **Test** against Phi-3-mini GGUF
4. **Verify** all 5 gates pass
5. **Generate** evidence package
6. **Document** results

---

## Current Status

| Stage | Status | Evidence |
|-------|--------|----------|
| VAL-019.1 | ✅ Complete | GGUF structure understood |
| VAL-019.2 | ⬜ Specified | Tensor access validated |
| VAL-019.3 | ⬜ Pending | Embedding execution |
| VAL-019.4+ | ⬜ Pending | Transformer primitives |

**Overall: 1 of 4 stages complete (25%)**
