--- ide_chatbot.html
+++ ide_chatbot_screenpilot_final.html
@@ -3,10 +3,9 @@
 
 <head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
-  <meta http-equiv="Content-Security-Policy"
-    content="default-src 'self' file: blob:; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdnjs.cloudflare.com blob:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; font-src 'self' https://fonts.gstatic.com data:; connect-src 'self' http://localhost:* ws://localhost:* http://127.0.0.1:*; img-src 'self' data: blob:;">
+  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; font-src 'self' data:; connect-src 'self'; img-src 'self' data: blob:; media-src 'self' blob:; frame-src 'self' http: https:; object-src 'none'; base-uri 'none'; form-action 'self'; frame-ancestors 'self';">
   <title>RawrXD IDE // AGENTIC INTERFACE</title>
   <link rel="icon"
     href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🦖</text></svg>">
   <!-- ================================================================
@@ -14,25 +13,12 @@
        All CDN resources have inline fallbacks below. When opened via file://
        or offline, the CDN loads will fail gracefully and the stubs activate.
        ================================================================ -->
   <!-- DOMPurify: XSS sanitization for all backend/assistant HTML output -->
-  <script src="https://cdnjs.cloudflare.com/ajax/libs/dompurify/3.0.6/purify.min.js" crossorigin="anonymous"
-    onerror="console.warn('[RawrXD] DOMPurify CDN blocked — shim will activate')"></script>
-  <!-- highlight.js: Syntax highlighting for code blocks in chat responses -->
-  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/atom-one-dark.min.css"
-    crossorigin="anonymous">
-  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js" crossorigin="anonymous"
-    onerror="console.warn('[RawrXD] highlight.js CDN blocked — syntax highlighting disabled')"></script>
-  <!-- Google Fonts: Non-blocking preload — falls back to system fonts in offline/file:// mode -->
-  <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>
-  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
-  <link rel="stylesheet"
-    href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;800&family=Inter:wght@400;500;600;700&display=swap"
-    media="print" onload="this.media='all'" onerror="this.remove()">
-  <noscript>
-    <link rel="stylesheet"
-      href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;800&family=Inter:wght@400;500;600;700&display=swap">
-  </noscript>
+<!-- highlight.js: Syntax highlighting for code blocks in chat responses -->
+<!-- Google Fonts: Non-blocking preload — falls back to system fonts in offline/file:// mode -->
+<noscript>
+</noscript>
   <!-- CDN Fallback: stubs for offline / file:// usage -->
   <script>
     // DOMPurify fallback — safe tag-stripping shim if CDN failed (offline / file:// mode)
     if (typeof DOMPurify === 'undefined') {
@@ -45,9 +31,9 @@
           'tr': 1, 'td': 1, 'th': 1, 'img': 1, 'hr': 1, 'details': 1, 'summary': 1, 'sub': 1, 'sup': 1
         };
         var SAFE_ATTRS = {
           'href': 1, 'src': 1, 'alt': 1, 'title': 1, 'class': 1, 'id': 1,
-          'style': 1, 'target': 1, 'rel': 1, 'colspan': 1, 'rowspan': 1, 'width': 1, 'height': 1
+          'style': 1, 'target': 1, 'rel': 1, 'colspan': 1, 'rowspan': 1, 'width': 1, 'height': 1, 'data-code-action': 1
         };
 
         window.DOMPurify = {
           version: '0.0.0-shim',
@@ -8380,11 +8366,11 @@
             title="Dual Agent: OFF — Toggle Architect↔Coder pipeline" style="opacity:0.5;">
             <span>&#x1F996;</span> <span id="dualAgentLabel">Dual</span>
           </div>
           <div class="toolbar-item" id="agenticModeBtn" onclick="toggleAgenticMode()"
-            title="Agentic Mode: ON — AI can read/write files, run commands, use git"
-            style="opacity:1.0; color:var(--accent-green);">
-            <span>&#x1F916;</span> <span id="agenticModeLabel">Agent: ON</span>
+            title="Agentic Mode: OFF — select BUILD or AGENT to grant workspace mutation authority"
+            style="opacity:0.5;">
+            <span>&#x1F916;</span> <span id="agenticModeLabel">Agent: OFF</span>
           </div>
           <div class="toolbar-item" id="voiceToolbarBtn" onclick="RawrVoice.toggleTTS()"
             title="Toggle TTS: Read AI responses aloud">
             <span>&#x1F50A;</span> <span id="voiceTTSLabel">TTS: OFF</span>
@@ -10990,9 +10976,9 @@
           timestamps: [],      // ring buffer of request timestamps
           blocked: 0,
         },
         inputGuard: {
-          maxLength: 1000000000,
+          maxLength: 4194304,  // 4 MiB production client ceiling
           blockedXss: 0,
           blockedLength: 0,
           blockedUrl: 0,
         },
@@ -13766,9 +13752,9 @@
         logDebug('Primary backend (' + State.backend.url + ') failed: ' + e.message, 'warn');
       }
 
       // ---- Try 1.5: RawrXD server on alternate port :8080 ----
-      if (State.backend.url.indexOf(':8080') === -1) {
+      if (!(State.screenpilot && State.screenpilot.authorityLocked) && State.backend.url.indexOf(':8080') === -1) {
         try {
           var altUrl = 'http://127.0.0.1:8080';
           logDebug('Trying RawrXD server at ' + altUrl + '...', 'info');
           var t0b = performance.now();
@@ -13827,9 +13813,9 @@
 
       // ---- Try 2: Direct Ollama fallback (default :11434) ----
       // Skip Ollama fallback when local models are loaded — user has their own engine.
       var hasLocalModels = State.model.list && State.model.list.some(function (m) { return m.type === 'gguf' || m.type === 'local'; });
-      if (!hasLocalModels && State.backend.ollamaDirectUrl && State.backend.ollamaDirectUrl !== State.backend.url) {
+      if (!(State.screenpilot && State.screenpilot.authorityLocked) && !hasLocalModels && State.backend.ollamaDirectUrl && State.backend.ollamaDirectUrl !== State.backend.url) {
         try {
           logDebug('Trying direct Ollama at ' + State.backend.ollamaDirectUrl + '...', 'info');
           const t0 = performance.now();
 
@@ -16059,11 +16045,11 @@
         var safe = '<div class="code-block">' +
           '<div class="code-header">' +
           '<span class="code-lang">' + escHtml(block.lang) + '</span>' +
           '<div class="code-actions">' +
-          '<button class="code-btn" onclick="copyCode(this)">Copy</button>' +
-          '<button class="code-btn-keep" onclick="keepCode(this)" title="Accept this code change">Keep</button>' +
-          '<button class="code-btn-undo" onclick="undoCode(this)" title="Revert this code change">Undo</button>' +
+          '<button class="code-btn" data-code-action="copy">Copy</button>' +
+          '<button class="code-btn-keep" data-code-action="keep" title="Accept this code change">Keep</button>' +
+          '<button class="code-btn-undo" data-code-action="undo" title="Revert this code change">Undo</button>' +
           '</div>' +
           '</div>' +
           '<div class="code-content"><pre><code class="hljs language-' + escHtml(block.lang) + '">' + highlightedCode + '</code></pre></div>' +
           '</div>';
@@ -16089,9 +16075,9 @@
       if (typeof DOMPurify !== 'undefined') {
         return DOMPurify.sanitize(text, {
           ALLOWED_TAGS: ['b', 'i', 'em', 'strong', 'p', 'br', 'ul', 'ol', 'li',
             'code', 'pre', 'span', 'div', 'button', 'table', 'thead', 'tbody', 'tr', 'th', 'td'],
-          ALLOWED_ATTR: ['class', 'style', 'onclick', 'title', 'data-lang', 'data-msg', 'data-block', 'id'],
+          ALLOWED_ATTR: ['class', 'style', 'title', 'data-lang', 'data-msg', 'data-block', 'data-code-action', 'id'],
           ALLOW_DATA_ATTR: true,
           KEEP_CONTENT: true
         });
       }
@@ -28558,139 +28544,11 @@
     // without triggering cross-origin errors.
     // ======================================================================
     (function () {
       'use strict';
-      var ALLOWED_ORIGINS = [
-        window.location.origin,
-        'http://localhost:11435',
-        'http://127.0.0.1:11435',
-        'http://localhost:11434',
-        'null'  // file:// protocol sends origin "null"
-      ];
-
-      var TestAPI = {
-        ping: function () { return { ready: true, url: location.href, ts: Date.now() }; },
-        getState: function () {
-          // Return a structured-clone-safe copy (strips DOM refs, Window, RegExp, functions)
-          try { return window.State ? JSON.parse(JSON.stringify(window.State)) : null; }
-          catch (e) { return window.State ? { backend: window.State.backend, model: window.State.model, chat: window.State.chat, gen: window.State.gen, security: { rateLimit: window.State.security.rateLimit, inputGuard: window.State.security.inputGuard } } : null; }
-        },
-        getBackendState: function () { return window.State ? window.State.backend : null; },
-        getModelState: function () { return window.State ? window.State.model : null; },
-        getConversation: function () {
-          // Conversation has methods — return a summary object instead
-          if (!window.Conversation) return null;
-          return { exists: true, addMessage: typeof window.Conversation.addMessage === 'function', getContextForAPI: typeof window.Conversation.getContextForAPI === 'function', clear: typeof window.Conversation.clear === 'function' };
-        },
-        getRawrVoice: function () { return window.RawrVoice ? window.RawrVoice.getState() : null; },
-        getRawrVoiceMetrics: function () { return window.RawrVoice ? window.RawrVoice.getMetrics() : null; },
-        getEngineAPI: function () { return window.EngineAPI ? true : false; },
-        getCoT: function () { return window.CoT ? { enabled: window.CoT.enabled, steps: window.CoT.steps.length } : null; },
-        getDualAgent: function () { return window.DualAgent ? true : false; },
-        hasFn: function (name) { return typeof window[name] === 'function'; },
-        hasObj: function (name) { return typeof window[name] !== 'undefined' && window[name] !== null; },
-        callFn: function () {
-          // Accept (name, ...fnArgs) — flatten to handle both apply and direct calls
-          // Harness convention: send('callFn', ['fnName', [arg1, arg2]])
-          // Bridge apply spreads to: callFn('fnName', [arg1, arg2])
-          // So a[1] is the args array — use it directly for apply()
-          var a = Array.prototype.slice.call(arguments);
-          var name = a[0];
-          var fnArgs = a.length === 2 && Array.isArray(a[1]) ? a[1] : a.slice(1);
-          if (typeof window[name] === 'function') {
-            var r = window[name].apply(null, fnArgs);
-            return r === undefined ? null : r;  // coerce undefined for postMessage
-          }
-          return null;
-        },
-        getElement: function (id) { var el = document.getElementById(id); return el ? { id: el.id, tag: el.tagName, visible: el.offsetParent !== null || (el.style && el.style.display !== 'none') } : null; },
-        // CSP-safe eval fallback — only for test bridge, guarded by origin check
-        eval: function (expr) {
-          try { return (new Function('return ' + expr))(); }
-          catch (e) { return { error: e.message }; }
-        },
-        // CSP-safe named commands (no eval needed)
-        getChildCount: function (id) { var el = document.getElementById(id); return el ? el.children.length : 0; },
-        getPerfHistoryLen: function () { return window.State && window.State.perf ? window.State.perf.history.length : 0; },
-        getStructuredLogLen: function () { return window.State && window.State.perf ? window.State.perf.structuredLog.length : 0; },
-        getSecurityLogLen: function () { return window.State && window.State.security ? window.State.security.eventLog.length : 0; },
-        clearConversation: function () { if (window.Conversation && window.Conversation.clear) { window.Conversation.clear(); return true; } return false; },
-        elements: function () {
-          var ids = ['chatMessages', 'chatInput', 'modelSelect', 'terminalInput', 'backendStatus', 'cotSwitch',
-            'debugPanel', 'perfPanel', 'securityPanel', 'agentDashboard', 'failurePanel', 'extensionPanel',
-            'endpointPanel', 'browserPanel', 'fileEditorPanel', 'gitPanel', 'rePanel'];
-          var result = {};
-          for (var i = 0; i < ids.length; i++) { result[ids[i]] = !!document.getElementById(ids[i]); }
-          return result;
-        },
-        libs: function () {
-          return {
-            DOMPurify: typeof DOMPurify !== 'undefined',
-            hljs: typeof hljs !== 'undefined',
-            mermaid: typeof mermaid !== 'undefined',
-            marked: typeof marked !== 'undefined'
-          };
-        }
-      };
-
-      window.addEventListener('message', function (event) {
-        if (ALLOWED_ORIGINS.indexOf(event.origin) === -1 && event.origin !== window.location.origin) return;
-        var data = event.data;
-        if (!data || !data.cmd || data.type === 'rawrxd-ide-ready') return;
-
-        var id = data.id;
-        var cmd = data.cmd;
-        var args = data.args || [];
-        var origin = event.origin === 'null' ? '*' : event.origin;
-        var source = event.source;
-
-        function sendReply(result, error) {
-          if (!source) return;
-          try {
-            source.postMessage({
-              id: id, cmd: cmd,
-              result: result === undefined ? null : result,
-              error: error || null,
-              ts: performance.now()
-            }, origin);
-          } catch (cloneErr) {
-            // DataCloneError — result not structured-cloneable (Promise, DOM, etc.)
-            source.postMessage({
-              id: id, cmd: cmd,
-              result: null,
-              error: 'Result not cloneable: ' + cloneErr.message,
-              ts: performance.now()
-            }, origin);
-          }
-        }
-
-        var result, error;
-        try {
-          if (TestAPI[cmd]) {
-            result = typeof TestAPI[cmd] === 'function' ? TestAPI[cmd].apply(null, args) : TestAPI[cmd];
-          } else {
-            error = 'Unknown command: ' + cmd;
-          }
-        } catch (e) {
-          error = e.message || String(e);
-        }
-
-        // Handle async results (Promises from callFn on async functions)
-        if (result && typeof result.then === 'function') {
-          result.then(function (v) { sendReply(v, null); })
-            .catch(function (e) { sendReply(null, e.message || String(e)); });
-        } else {
-          sendReply(result, error);
-        }
-      });
-
-      // Signal ready to parent frame
-      if (window.parent && window.parent !== window) {
-        try {
-          window.parent.postMessage({ type: 'rawrxd-ide-ready', url: location.href, ts: Date.now() }, '*');
-        } catch (e) { /* cross-origin, bridge will still work via postMessage */ }
-      }
-      console.log('[TestBridge] PostMessage bridge initialized');
+      // Shipping build: privileged postMessage/eval harness intentionally disabled.
+      // Use tests/static_audit.py and the HTTP certification gates instead.
+      console.log('[TestBridge] disabled in shipping ScreenPilot build');
     })();
   </script>
 
   <!-- ==================================================================
@@ -30432,11 +30290,9 @@
     window.MWM = MWM;
     console.log('[RawrXD] Multi-Window Manager loaded — Ctrl+N to open new window');
   </script>
 
-  <!-- Action Item #20: Chat Command Router — exposes /p, /preset, /cot, /fix, /agent etc. -->
-  <script src="ide_chat_commands.js"
-    onerror="console.warn('[RawrXD] ide_chat_commands.js not found — /commands disabled')"></script>
-  <script src="/gui/rawrxd-nav.js"></script>
+  <!-- RAWRXD_SCREENPILOT_E2E_FINAL: canonical in-process authority client -->
+  <script src="/screenpilot/screenpilot_authority.js"></script>
 </body>
 
 </html>