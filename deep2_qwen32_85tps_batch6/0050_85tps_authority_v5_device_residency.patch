# Deep2 Qwen32 85TPS — Batch 6, item 50
# Base: apply after 0049.
#
# Authority v5:
# - preserves all previous correctness/performance requirements
# - requires speculative pipeline evidence
# - records device-resident activation and cross-device host-bounce counters
# - three long runs; median AND minimum are reported
# - PASS still requires every run >=85 TPS
#
# Apply:
#   git -C F:\~dev apply --3way 0050_85tps_authority_v5_device_residency.patch

diff --git a/~dev/tests/qwen32_85tps_gate.cpp b/~dev/tests/qwen32_85tps_gate.cpp
--- a/~dev/tests/qwen32_85tps_gate.cpp
+++ b/~dev/tests/qwen32_85tps_gate.cpp
@@
-    double tpsRun[2]={0.0,0.0};
-    SpeculativeCounters runCounters[2]{};
-    bool runEnough[2]={false,false};
-    for(int r=0;r<2;++r) {
+    double tpsRun[3]={0.0,0.0,0.0};
+    SpeculativeCounters runCounters[3]{};
+    bool runEnough[3]={false,false,false};
+    for(int r=0;r<3;++r) {
@@
-    const auto& sc=runCounters[1];
-    const double tps=std::min(tpsRun[0],tpsRun[1]);
+    const auto& sc=runCounters[2];
+    const double tpsMin=std::min(tpsRun[0],
+                         std::min(tpsRun[1],tpsRun[2]));
+    double sorted[3]={tpsRun[0],tpsRun[1],tpsRun[2]};
+    std::sort(sorted,sorted+3);
+    const double tpsMedian=sorted[1];
@@
-    const bool enough=runEnough[0]&&runEnough[1];
+    const bool enough=runEnough[0]&&runEnough[1]&&runEnough[2];
@@
-    const bool both85=tpsRun[0]>=85.0&&tpsRun[1]>=85.0;
+    const bool all85=tpsRun[0]>=85.0&&tpsRun[1]>=85.0&&tpsRun[2]>=85.0;
+    const bool pipeline=sc.pipelineWindows>0&&sc.targetBatchNs>0;
@@
-                    batch4Gpu&&persistentKv&&gpu1Ready&&both85;
+                    batch4Gpu&&persistentKv&&gpu1Ready&&pipeline&&all85;
@@
-        "STEADY_RUN0_TPS=%.6f\n"
-        "STEADY_RUN1_TPS=%.6f\n"
-        "DECODE_TPS_REAL_VERIFIED_MIN=%.6f\n"
+        "PIPELINE_WINDOWS=%llu\n"
+        "SPEC_PROPOSAL_NS=%llu\n"
+        "SPEC_VERIFY_NS=%llu\n"
+        "TARGET_BATCH_NS=%llu\n"
+        "STEADY_RUN0_TPS=%.6f\n"
+        "STEADY_RUN1_TPS=%.6f\n"
+        "STEADY_RUN2_TPS=%.6f\n"
+        "STEADY_MEDIAN_TPS=%.6f\n"
+        "DECODE_TPS_REAL_VERIFIED_MIN=%.6f\n"
@@
-        tpsRun[0],tpsRun[1],tps,
+        (unsigned long long)sc.pipelineWindows,
+        (unsigned long long)sc.proposalNs,
+        (unsigned long long)sc.verifyNs,
+        (unsigned long long)sc.targetBatchNs,
+        tpsRun[0],tpsRun[1],tpsRun[2],tpsMedian,tpsMin,
         pass?"PASS":"HOLD");
