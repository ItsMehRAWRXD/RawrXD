/* RxRunStateHooks.cpp — TU anchor (header-inline hooks). ≤40. */
#include "RxRunStateHooks.hpp"

namespace rxow {
/* Force a symbol for MSVC link of standalone test. */
int RxOw_ForceLink = 1;
}
