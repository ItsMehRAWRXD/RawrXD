services:
  engine:
    build:
      context: .
      dockerfile: docker/engine.Dockerfile
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    ports:
      - "11435:11435"
