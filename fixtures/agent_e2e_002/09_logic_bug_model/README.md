# 09_logic_bug_model — Runtime logic bug (GGUF/model-directed gate)
PROMPT: Fix the failing test so add(2,3)==5 and print hello from e2e09
EXPECTED: autofix build-only path will NOT repair (build succeeds). Marks model-directed gap.
