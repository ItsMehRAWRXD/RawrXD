GATE=P1_POLICY_SHA_LIFECYCLE_DIAG_001
STATUS=CONTRACT_FROZEN
EFFECTIVE=2026-08-29
DEPENDS_ON=P1_REPEATABILITY_TRIAL_VALIDITY_DIAG_001 (FIRST_FALSE=POLICY_SHA_STABLE)

Purpose
-------
Determine WHY PolicySha256 changes across the measured trial lifecycle and
which hashed fields move at each transition. Does NOT modify acceptance rules,
InferenceEngine, or P1_REAL_SPEEDUP_001/GATE.txt.

Frozen references
-----------------
P1_REAL_SPEEDUP_001               PASS / CLOSED (GATE_SHA=04451DB1…)
Frozen speedup EXE                7DFDA62A55A092B59EA223D0604F27DAEB19417FA86681BEFDB164DDDEF7DBD1
Known failure signature           AT_SEAM=aabe7bdb… AFTER=9ea709bb… (AT_SEAM==AFTER FAIL)

Lifecycle samples (per trial)
-----------------------------
POLICY_SHA_PRE_GENERATE           after ApplyProfile, before Load
POLICY_SHA_PRE_LOAD               same anchor (explicit)
POLICY_SHA_POST_LOAD              after Load / EnforcePolicyOnIdeLoad
POLICY_SHA_AT_CERTIFIED_SEAM      after session.Initialize (MODEL_READY)
POLICY_SHA_GENERATE_ENTRY         immediately before Generate()
POLICY_SHA_FIRST_TOKEN            first streaming token callback
POLICY_SHA_GENERATE_EXIT          immediately after Generate() returns
POLICY_SHA_AFTER                  alias of GENERATE_EXIT in this harness
POLICY_SHA_POST_ENSURE            after explicit EnsurePolicyLoaded() post-generate

Per transition record
---------------------
POLICY_SHA_<FROM>_TO_<TO>
FIELDS_CHANGED=[...]              only PolicySha256-mixed fields
VERSION_<FROM> / VERSION_<AFTER>

Classification targets
----------------------
PRE → SEAM mutation         allowed if load normalization owns it
SEAM → AFTER mutation       forbidden if policy frozen at certified seam
AT_SEAM sampling early      fix diagnostic/harness seam point
SHA domain includes mutable state   fix hash domain
Runtime mutation after seam         runtime provenance defect

Rules
-----
No InferenceEngine / runtime performance edits
Diagnostic harness only (separate EXE)
Do not reopen P1_REAL_SPEEDUP_001
Do not unblock tail latency gate

Evidence
--------
evidence/P1_POLICY_SHA_LIFECYCLE_DIAG_001/
  CONTRACT.txt GATE.txt LIFECYCLE.txt run.log ENVIRONMENT.txt
