GATE=DEEP2_DUAL_AGGREGATE_DECODE_FINISH_BALANCE
STATUS=SOURCE_DROP_NOT_RUN
PRODUCT_LINKED=0
PROMOTE=0

AUTHORITY_INPUT_BASELINE:
  AGGREGATE_BW_AUTHORITY=1
  MATERIAL_SAME_TOKEN_OVERLAP=1

OPTIMIZATION_AUTHORITY:
  PRIMARY=min(median(CRITICAL_PATH_NS))
  SECONDARY=min(median(abs(FINISH_SKEW_NS)))
  NOT_AUTHORITY=max(OVERLAP_NS alone)
  NOT_AUTHORITY=nominal ROWS_SPLIT

FAIL_CLOSED_IF:
  OUTPUT_PARITY!=1
  DEVICE_OK!=1
  SAME_TOKEN!=1
  PRODUCT_LINKED!=1
  PACKED_Q2K_LIVE!=1
  SERIAL_GPU_CHAIN!=0
  WEIGHT_MIGRATION!=0

NO_DOUBLE_CORRECTION=1
ROW_SPLIT_AND_REPS_SPLIT_MUST_SHARE_ONE_EFFECTIVE_WORK_AUTHORITY=1
