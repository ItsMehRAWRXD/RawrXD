FOUNDATION_F1=OPEN (law sealed; Codestral chase continues)

SEPARATE_MODELS:
  Qwen2.5-Coder-14B: hidden=5120 layers=48 heads=40 kv=8 ffn=13824 vocab=152064
  Codestral-22B:     hidden=6144 layers=56 heads=48 kv=8 ffn=16384 vocab=32768
  6144 is Codestral hidden — not a bug by itself.

HOST_DECODE_SKIP_LIVEPATH:
  MEANS_SKIP_ENHANCEMENTS_ONLY=1
  MEANS_SKIP_BASE_FORWARD=0

F-2 MODEL FACTS:
  vocab and hidden are independent fields
  GGUF infer no longer assumes dim0=vocab (was coupling vocab:=hidden=5120)

F-1 EXEC INSTANCE:
  RawrModelExec.hpp — no static model geometry
  Activation writeGeneration required for logits readiness

F0 RMV:
  registeredBytes (~15.7GB) != maxResident (512MB) — CORRECT; do not "fix"

F1 BASE FORWARD:
  embed → all layers → final norm → logits
  forwardTokenAllLayers refuses zero-norm "success" (BASE_FORWARD_ZERO_HIDDEN)

CODESTRAL CHASE:
  EMBED_NORM ok → PREFILL_DONE → FINAL_HIDDEN_NORM=0 at logits
  Inspect: which producer owns last-prompt hidden; layer count=56;
  ping-pong buffer; final RMSNorm write; pointer into computeLogits

HIERARCHY:
  F-3 ABI … F1 BASE FORWARD … F2 CAPABILITY … F7 PRODUCT
  Shared-x / ROWS / KVA sit ABOVE a non-skippable base forward.
