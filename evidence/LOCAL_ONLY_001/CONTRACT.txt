LOCAL_ONLY_001 — FROZEN CONTRACT
Authorized: 2026-08-30
Authority: RawrXD Audit — Local-Only Coding-Agent Baseline
Tree: F:\~dev\rawrxd (canonical for this gate)

FROZEN SCOPE
```
GATE                    = LOCAL_ONLY_001
STATUS                  = PASS / CERTIFIED (see GATE.txt)
INFERENCE_BACKEND       = Deep2/GGUF only
OLLAMA_CLIENT_PRESENT   = NO
PORT_11434_REACHABLE    = NO
FALLBACK_BEHAVIOR       = FAIL_CLOSED
NETWORK_FALLBACK        = FORBIDDEN
```

PASS CONDITION
```
A generated inference request succeeds with Deep2/GGUF while ollama.exe is
absent and 127.0.0.1:11434 is unreachable; every fallback returns a hard
diagnostic, never response-like text.
```
CLOSED: 2026-08-30 — EXIT=0 — evidence/LOCAL_ONLY_001/GATE.txt

OUT OF SCOPE (do not disturb)
```
P1_UI_MENU_E2E_001      = BLOCKED (independent)
STREAMER_CERT_001       = PASS / CERTIFIED (evidence untouched)
STREAMER_PERF_BASELINE  = historical ~1.3 E2E tok/s (do not overwrite)
EGRESS_001              = OPEN — whole-product egress / Ollama quarantine
STATIC_DEP_001          = after EGRESS — shipping EXE strings/symbols proof
MODEL_IDENTITY_001      = separate subsequent gate
```

CLAIM BOUNDARY
```
LOCAL_ONLY_001 certifies Deep2/GGUF independence from Ollama on the selected
inference path only. It does NOT certify whole-product Ollama absence,
default-deny egress, or a “no-dependency product” shipping claim.
```

REQUIRED ARTIFACTS
```
CONTRACT.txt                      (this file — frozen)
OLLAMA_DEPENDENCY_INVENTORY.txt   (static shipping-path inventory)
GATE.txt                          (written only on PASS/FAIL close)
PROOF_RUN.txt                     (runtime capture)
cert harness stdout/stderr        (when built)
```

IMPLEMENTATION RULES
```
1. Production inference must not construct an Ollama HTTP client.
2. Empty / cleared Ollama URL must not resurrect 127.0.0.1:11434.
3. trySendToOllama and equivalent WinHTTP :11434 paths fail closed.
4. Stub / placeholder “AI” text on Ollama miss is forbidden.
5. Deep2/GGUF success path is the only allowed response source for this gate.
```
