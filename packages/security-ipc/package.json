{
  "name": "@rawrxd/security-ipc",
  "version": "0.1.0",
  "description": "JSON-RPC IPC contracts for Sovereign sandbox broker",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.2.0",
    "typescript": "^5.9.3"
  }
}
